#include<iostream>
using namespace std;
int main()
{
	int arr[]={4,5,3,2,8,9,1,11};
	int i,l=arr[0],sl=sl=arr[0];
	
	for(i=1;i<8;i++)
	{
		if(arr[i]<l && arr[i]>sl){
			sl=arr[i];
		}
		if(arr[i]>l)
		{
			sl=l;
			l=arr[i];
		}
	}
	cout<<sl;
}
